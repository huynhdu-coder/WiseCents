# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - wisecents-backend-dev

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'   # Recommended for Azure App Service

      - name: Install backend dependencies
        run: npm install
        working-directory: server

      # Only package backend code (do NOT package client/)
      - name: Upload backend artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend
          path: server/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download backend artifact
        uses: actions/download-artifact@v4
        with:
          name: backend

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_5E38F34B62024CF18CE49F36DF168D7C }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_9B0E92EA9C894D048D043326AFF9AD11 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_C70CC087FDDA40219FD64A387644FF7D }}

      - name: Deploy backend to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'wisecents-backend-dev'
          slot-name: 'Production'
          package: .


